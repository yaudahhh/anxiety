### install sc

```
apt update && apt upgrade -y && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/yaudahhh/anxiety/main/setup.sh && chmod +x setup.sh && ./setup.sh
```
